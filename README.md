# url-analyzer
The URL Analyzer is a Java-based project that analyzes and processes URLs using basic string manipulation and control structures. It validates URL format, extracts components like protocol and domain, and demonstrates core Java concepts such as loops, conditionals, and logical problem-solving.
